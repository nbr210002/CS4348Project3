public class BTreeNode
{
    // Represents one node in the B-Tree
    // Will read and write itself, detect overflow
    // Will split and return a new key and node and insert key value into a node
}
